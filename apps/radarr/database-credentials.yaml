---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-password-source
  namespace: database
  labels:
    cnpg.io/reload: "true"   
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore
  target:
    name: radarr-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: radarr
        property: db_user

    - secretKey: password
      remoteRef:
        key: radarr
        property: db_password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-db-secret
  namespace: radarr
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore

  data:
    - secretKey: username
      remoteRef:
        key: radarr
        property: db_user

    - secretKey: password
      remoteRef:
        key: radarr
        property: db_password
