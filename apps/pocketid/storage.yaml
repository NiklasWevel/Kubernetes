apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pocketid-data-pvc
  namespace: pocketid
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: share
  volumeName: pocketid-pv
