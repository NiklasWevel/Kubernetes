---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openwebui-db-secret
  namespace: openwebui
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore

  data:
    - secretKey: username
      remoteRef:
        key: openwebui
        property: db_user
    - secretKey: password
      remoteRef:
        key: openwebui
        property: db_password

  target:
    name: openwebui-db-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        DATABASE_URL: >-
          postgresql://{{ .username }}:{{ .password }}@postgres-cluster-rw.database.svc.cluster.local:5432/openwebui-db
        DB_USER: "{{ .username }}"
        DB_PASSWORD: "{{ .password }}"
