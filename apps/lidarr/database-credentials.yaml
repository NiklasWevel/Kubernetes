apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lidarr-db-secret
  namespace: lidarr
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore

  data:
    - secretKey: username
      remoteRef:
        key: lidarr
        property: db_user

    - secretKey: password
      remoteRef:
        key: lidarr
        property: db_password
