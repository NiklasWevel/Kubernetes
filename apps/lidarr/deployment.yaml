---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lidarr
  namespace: lidarr
  labels:
    app: lidarr
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: lidarr
  template:
    metadata:
      labels:
        app: lidarr
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: lidarr
          image: docker.wevelsiep.com/linuxserver/lidarr:3.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8686
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: true
            readOnlyRootFilesystem: false
            seccompProfile:
              type: "RuntimeDefault"
          readinessProbe:
            httpGet:
              path: /ping
              port: 8686
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: 8686
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: lidarr-config
              mountPath: /config
            - name: lidarr-media
              mountPath: /media
            - name: lidarr-seedbox
              mountPath: /downloads
            - name: lidarr-logs
              mountPath: /config/logs
            - name: lidarr-configmap
              mountPath: /config/config.xml
              subPath: config.xml
      volumes:
        - name: lidarr-config
          persistentVolumeClaim:
            claimName: lidarr-pvc
        - name: lidarr-media
          persistentVolumeClaim:
            claimName: lidarr-media-pvc
        - name: lidarr-seedbox
          persistentVolumeClaim:
            claimName: lidarr-seedbox-pvc
        - name: lidarr-configmap
          secret:
            secretName: lidarr-configmap-secret
        - name: lidarr-logs
          emptyDir:
            medium: Memory
            sizeLimit: 32Mi
