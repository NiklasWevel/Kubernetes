---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wikijs-db-secret
  namespace: wikijs
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore

  data:
    - secretKey: username
      remoteRef:
        key: database/wikijs
        property: db_user

    - secretKey: password
      remoteRef:
        key: database/wikijs
        property: db_password
