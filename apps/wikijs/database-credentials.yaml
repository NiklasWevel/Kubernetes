---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: wikijs-db-secret
  namespace: wikijs
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore

  data:
    - secretKey: DB_USER
      remoteRef:
        key: database/wikijs
        property: db_user

    - secretKey: DB_PASS
      remoteRef:
        key: database/wikijs
        property: db_password
