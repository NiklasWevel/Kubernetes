apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unpackerr-seedbox-pvc
  namespace: unpackerr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: "share"
  volumeName: unpackerr-seedbox-pv
