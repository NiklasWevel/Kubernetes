---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarr-db-secret
  namespace: sonarr
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore

  data:
    - secretKey: username
      remoteRef:
        key: sonarr
        property: db_user

    - secretKey: password
      remoteRef:
        key: sonarr
        property: db_password
