apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openwebui-password-source
  namespace: openwebui-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore
  target:
    name: openwebui-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: database/openwebui
        property: db_user
    - secretKey: password
      remoteRef:
        key: database/openwebui
        property: db_password
