apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: suggestarr-password-source
  namespace: suggestarr-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kubernetes
    kind: SecretStore
  target:
    name: suggestarr-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: database/suggestarr
        property: db_user
    - secretKey: password
      remoteRef:
        key: database/suggestarr
        property: db_password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-superlogin
  namespace: suggestarr-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kubernetes
    kind: SecretStore
  target:
    name: postgres-superlogin
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: database/suggestarr
        property: db_root_user
    - secretKey: password
      remoteRef:
        key: database/suggestarr
        property: db_root_password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres-s3-secret
  namespace: suggestarr-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kubernetes
    kind: SecretStore
  target:
    name: postgres-s3-secret
    creationPolicy: Owner
  data:
    - secretKey: s3_key_id
      remoteRef:
        key: database/suggestarr
        property: s3_key_id
    - secretKey: s3_secret_key
      remoteRef:
        key: database/suggestarr
        property: s3_secret_key
