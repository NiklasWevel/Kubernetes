apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lidarr-password-source
  namespace: lidarr-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore
  target:
    name: lidarr-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: database/lidarr
        property: db_user
    - secretKey: password
      remoteRef:
        key: database/lidarr
        property: db_password
