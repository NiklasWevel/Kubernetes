apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: seerr-password-source
  namespace: seerr-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore
  target:
    name: seerr-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: database/seerr
        property: db_user
    - secretKey: password
      remoteRef:
        key: database/seerr
        property: db_password
