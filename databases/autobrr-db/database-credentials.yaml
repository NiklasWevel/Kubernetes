apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: autobrr-password-source
  namespace: autobrr-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore
  target:
    name: autobrr-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: database/autobrr
        property: db_user
    - secretKey: password
      remoteRef:
        key: database/autobrr
        property: db_password
