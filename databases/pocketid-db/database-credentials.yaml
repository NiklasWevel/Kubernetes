apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pocketid-password-source
  namespace: pocketid-db
  labels:
    cnpg.io/reload: "true"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-global
    kind: ClusterSecretStore
  target:
    name: pocketid-db-secret
  data:
    - secretKey: username
      remoteRef:
        key: database/pocketid
        property: db_user
    - secretKey: password
      remoteRef:
        key: database/pocketid
        property: db_password
