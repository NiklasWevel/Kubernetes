apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: oauth2-wan
  namespace: oauth2
spec:
  entryPoints:
    - websecure-wan-traffic
  routes:
    - match: Host(`auth.wevelsiep.com`)
      kind: Rule
      services:
        - name: oauth2
          port: 4180
