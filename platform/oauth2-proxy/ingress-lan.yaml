apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: oauth2-lan
  namespace: oauth2
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.wevelsiep.com`)
      kind: Rule
      services:
        - name: oauth2
          port: 4180
