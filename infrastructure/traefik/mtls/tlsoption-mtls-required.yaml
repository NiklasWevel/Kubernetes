apiVersion: traefik.io/v1alpha1
kind: TLSOption
metadata:
  name: mtls-required
  namespace: traefik
spec:
  minVersion: VersionTLS13
  clientAuth:
    secretNames:
      - mtls-client-ca
    clientAuthType: RequireAndVerifyClientCert
