apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik
resources:
  - certificate.yaml
  - helmrelease.yaml
  - helmrepository.yaml
configMapGenerator:
  - name: traefik-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml
