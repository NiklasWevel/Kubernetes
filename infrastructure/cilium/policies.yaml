apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-cilium-policies
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure-cilium
  interval: 10m
  path: ./infrastructure/cilium/network-policies
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
