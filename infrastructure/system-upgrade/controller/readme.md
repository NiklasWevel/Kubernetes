ToDo:
Fix controller FluxCD Kustomization. At the moment the controller is installed by running


```bash
k apply -f https://github.com/rancher/system-upgrade-controller/releases/download/v0.19.0/system-upgrade-controller.yaml 
```

locally.
