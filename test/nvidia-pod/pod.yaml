apiVersion: v1
kind: Pod
metadata:
  name: nvidia-test
  namespace: testing
spec:
  containers:
    - name: cuda-vector-add
      image: "nvcr.io/nvidia/k8s/cuda-sample:vectoradd-cuda11.2"
      resources:
        limits:
          nvidia.com/gpu: 1
  restartPolicy: OnFailure
