apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-clusterrolebinding
  namespace: flux-system
spec:
  interval: 5m0s
  path: ./infrastructure/clusterrolebinding
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system